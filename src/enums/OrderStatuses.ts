enum OrderStatuses {
    created = 'created',
    manager_reviw = 'manager_reviw',
    in_progress = 'in_progress',
    delivered = 'delivered',
    cancelled = 'cancelled'
}

export default OrderStatuses;